{
  "name": "@tsx-use/state-with-history",
  "version": "0.1.0",
  "description": "Takes an `initialState: T` and produces an object that has properties `initial`, `previous`, & `current`, as well as methods `set` (current), `revert` (to previous), & `reset` (to initial).",
  "main": "index.js",
  "scripts": {
    "test": "vitest"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/tsx-use/state-with-history.git"
  },
  "keywords": [
    "react",
    "typescript",
    "hook",
    "hooks",
    "state",
    "history",
    "previous"
  ],
  "author": "Xs-and-10s",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/tsx-use/state-with-history/issues"
  },
  "homepage": "https://github.com/tsx-use/state-with-history#readme",
  "devDependencies": {
    "@testing-library/react": "^16.1.0",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
